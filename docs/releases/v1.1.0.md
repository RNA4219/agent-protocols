# agent-protocols v1.1.0

## Summary

- `workflow-cookbook` からの `Evidence` 連携導線を追加
- README / protocol / operations に受け側の参照手順を追加

## Highlights

- `StructuredLogger` plugin ベースの `Evidence` 出力導線を明文化
- plugin guide / sample config / consumer sample への相互リンクを追加
- `workflow-cookbook` を使った受け側監査観点を `operations.md` に追記

## Validation

- docs change only
- `workflow-cookbook` 側では `Evidence` plugin と sample config のテストを通過済み
